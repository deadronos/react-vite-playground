


export type Position = { x: number; y: number; z: number };
export type Velocity = { x: number; y: number; z: number };
export type Health = { health: number };
export type Asteroid = { asteroid: true };
export type Turret = { turret: true; cooldown: number };
